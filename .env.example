# Database
DB_PASSWORD=change_me_to_a_secure_password

# CORS origin (your domain or IP)
CORS_ORIGIN=http://your-domain.com

# API URL for frontend build (leave empty if using nginx proxy)
VITE_API_URL=
